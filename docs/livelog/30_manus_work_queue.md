# Manus 作業キュー（優先順・2026-06-22）

Manusが手すきになり次第、上から。各項目の詳細指示は参照先に。

## 0. いま進行中：Gmail Step3 JSONパースバグ修正（最優先）
- **A案＝de-fence＋ブレース切り出しヘルパーを Step2/Step3 共用**（前回指示済み）。
- **＋プロンプト強化（今回追加）**：classifyEmail(Step2)・extractEmailData(Step3)の
  system末尾に必ず以下を入れる（パース堅牢化との二重対策）：
  ```
  出力は純粋なJSONのみ。コードブロック(```やjson)・前置き・説明文・末尾コメントを
  一切付けない。最初の文字が { で、最後の文字が } であること。
  ```
- 修正後、**初回バックフィルを再実行**（前回の29件はStep3エラーで取りこぼした過少値）。
- E2E確認：取得した公演名/日付/会場が正しいか・既知の網漏れ2通(eplus.co.jp/pia.co.jp)が
  入るか・宣伝の非混入・本文非保存。真の取得件数を報告。

## 1. Gmail取込の仕上げ 残り（doc28）
- 残骸クリーンアップ（yukato連携・テストデータ・幽霊カウント1）／初回バックフィル保証＋
  lastSyncAtロジック／同期UX（状態連動・離脱OK明示・二重起動防止）／publicId /u/1 確認。

## 2. liverico.app UI バグ（#memolog 積み残し）
- **ロゴ表示＝案3で静的配信に統一**（storageProxyを経由させない・ロゴ/OGP/aiconはpublic/）。
- 上部タブのスワイプ誤タップ→タップ可能なタブUIに。

## 3. モニター加藤さん実機バグ（#memolog 積み残し）
- ①「ご要望・質問」入力欄に文字が打てない ②Gmail同期UX（上記1と統合）③幽霊カウント1（残骸掃除）。

## 4. ビジュアル①② フェーズ0前倒し（doc29）
- ①フライヤー（保存・表示・公開範囲は友達まで初期値）／②チケット画像（個人情報→デフォルト非公開）。

## 5. Apple Music アー写カバー率検証（doc29・ルール3）
- tools/apple_music_eval を 3認証情報(env)で実行→「アー写OK n/16」比較表＋前髪ぱっつん少年が
  取れるか報告→良ければアー写＋予習プレイリスト実装。

## 6. 初回バックフィルの体感改善（モニター配布前・急がない＝快適さの話）
初回バックフィル（過去5年・千件級）はキャッシュ無しだと全件LLM一次判定で遅い（増分は速い・初回のみ）。
登録直後の第一印象が「重い」になるのを防ぐ改善。動くかではなく快適さの話なので緊急ではないが、
モニター配布前に入れたい。
- 進捗に**残り時間の目安（ETA）**を出す。
- **取れたものから順次プレビューに出す**（全件完了まで待たせない）。
- Step2の一次判定を**並列化/バッチ**して速くする（現状は逐次っぽい・レート制限と両睨み）。

## 不変条件
- LLM出力は純JSONとは限らない＝**必ずde-fence＋ブレース切り出しでパース**（JSON恒久対策と同family）。
- 本文非保存／調査はメタデータのみ／本番を汚さない（dev serverで検証）。
